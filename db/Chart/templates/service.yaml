apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}-service
  labels:
    app: {{ .Values.name }}
spec:
  ports:
  - port: 5432
    targetPort: 5432
  selector:
    app: {{ .Values.name }}